/**
 * Order domain: placed orders with locked prices and the status state machine,
 * plus short-lived checkout reservations. The availability ledger counts
 * non-cancelled order items and live reservations.
 */
package hu.deposoft.webshop.domain.order;
