# Pin the public npm registry for reproducible, credential-free builds
# (Railway/CI). Do NOT point this at a private/internal registry — the deploy
# build has no access to one. Personal registry config stays in ~/.npmrc.
registry=https://registry.npmjs.org/
